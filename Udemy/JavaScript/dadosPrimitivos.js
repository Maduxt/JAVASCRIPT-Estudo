// Tipos de dados primitivos: String, Number, Undefinid.

//SÃO IMUTÁVEIS.

//String, pode ser CONSTANTE OU VARIÁVEL;
const nome = "nome";
let nomes = `nomes`;
let nomess = 'nomess';

//Number, pode ser CONSTANTE OU VARIÁVEL;

const numberOne = 1;
let numberTwo = 10.90;

//Undefinid e Null, pode ser CONSTANTE ou VARIÁVEL;

const bathroomOne = null; //sem o null geralmente é um erro pois esquecemos ou não definimos essa variável.
let bathroomTwo = null; //utilizamos para desconfigurar uma variável, ex: quando queremos que o usuário selecione uma cor e ele não escolhe, não definiu cor.

//Boolean - True e False (lógico), pode ser CONSTANTE ou VARIÁVEL;

const aprovado = false;
const reprovado = true;

console.log (typeof aprovado);